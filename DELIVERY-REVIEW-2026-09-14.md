# 财经日报漏发诊断与修复验收方案

## 已核实的证据

- 仓库：emilyyyho/gpt-finance-daily-briefing，默认分支 main。
- 2026-09-14 的 ChatGPT 任务记录报告 NewsNow 访问失败、GitHub 写入返回 403 Resource not accessible by integration。这是任务记录的错误报告，尚未取得该连接器原始请求日志。
- 仓库最近提交与 Actions 运行均停留在 2026-09-13。没有 9 月 14 日日报提交，也没有今早的飞书投递运行。
- 昨日测试运行 34764610141 成功，发送 reports/test-2026-09-13.md；日志显示 1 条 Markdown 卡片发送成功。不是今天真实日报的端到端验证。
- FEISHU_WEBHOOK_URL Secret 名称存在；未读取或导出其值。
- 仓库 Issues 功能关闭，未找到 Issue；失败记录目前来自“财经日报定时任务”的对话。
- 现场只读请求 cls-telegraph、wallstreetcn-quick、jin10 均返回 HTTP 200，各有 30 条。该结果不能证明早上同一环境也可访问，也不能证明每条新闻均符合筛选规则。
- 当前 CLI 对仓库有 ADMIN 权限，不代表另一个 ChatGPT 集成有内容写入权限。

## 根因与潜在缺陷

1. 所有发送依赖 ChatGPT 先生成并成功提交。仓库工作流只有 push 和手动触发，缺少独立云端采集和定时兜底。
2. ChatGPT 记录为 08:30/20:30，仓库 JSON、校验器和文档仍为 20:30 单次；原需求为 08:00/20:00。
3. 所有 NewsNow 地址共用一个域名，多个 source_id 不等于独立备用数据源。
4. 早晚共用 reports/YYYY-MM-DD.md，缺少独立期次和发送状态，不利于重试、审计与防重。
5. 工作流只取最后提交中第一个 Markdown 路径，多文件或多提交推送可能漏发。
6. send_feishu.py 曾把非 JSON、缺少 code 的 HTTP 200 当作成功。
7. 按 16000 个字符拆分中文日报可能产生过大的 UTF-8 JSON 消息。
8. 没有逐期送达核验、有限重试和持续失败告警。网络超时可能发生在服务端已接收之后，简单重试无法保证恰好一次。

## 已准备的本地修复

- 发送端要求明确的成功状态码；非法 JSON、缺少状态码和错误码均失败。
- 缩小分片并检查 UTF-8 JSON 字节数，发送前检查所有分片。
- 超时标记为送达未确认，不盲目整篇重发；传输异常不输出 Webhook 信息。
- 新增四组回归测试并纳入 CI；本地全部通过，旧配置检查及卡片 dry-run 通过。
- 尚未提交远端、部署、发送测试消息或修改现有 ChatGPT 定时任务。

## 待审核的运行方案

保留现有飞书群机器人与 GitHub，先不引入付费模型或新云平台。

1. 统一早晚时间，分别存储 reports/YYYY-MM-DD-am.md 与 reports/YYYY-MM-DD-pm.md。
2. 免费云端任务独立抓取并保存时间明确的新闻，增加不同服务商的可用备用源。无证据的内容不补造，数量不足如实标记。
3. ChatGPT 分析作为增强版本；需先验证 Scheduled Task 的真实写入权限，不能以 CLI 成功代替连接器测试。
4. 若用户接受，AI 分析失败时发送有来源的基础新闻版，并明确分析缺失；不是投资研判完成。
5. 所有发送入口共用期次锁与分片状态；已确认成功的分片跳过，确定失败有限重试，状态不明单独记录。机器人 Webhook 没有端到端幂等保证时，不能承诺绝对无重复。
6. 每期结束检查是否成功；漏期时补发当期，数据过期则提示异常，不发送旧日报冒充当期。
7. GitHub Actions 云端不依赖本地电脑，但 schedule 可能排队，不能承诺整点 SLA。如必须严格准点，另行评估独立免费调度，并先实测。

## 上线验收

- CI：时区和期次边界、新闻时间过滤和去重、全部源失败、部分源失败、多文件报告、非法响应、中文长文分片、部分分片成功、重复触发测试。
- 手动完整链路：云端抓取真实新闻 -> 当期文件 -> 飞书确认响应 -> 保存状态；再运行同一期验证防重。
- 关闭本地电脑后验证至少一个云端期次。
- 连续七天检查十四个期次的计划时间、实际开始、采集完成、发送结果、补发次数。
- 每天两次是目标；全源不可用时只能异常通知，不能把异常通知算作成功新闻日报。
